[package]
name = "anim_to_vtk"
version = "0.1.0"
edition = "2021"
description = "Convert OpenRadioss animation files to legacy VTK format (ASCII or binary)"
license = "MIT"

[dependencies]
ryu = "1.0"

[dev-dependencies]
tempfile = "3"
